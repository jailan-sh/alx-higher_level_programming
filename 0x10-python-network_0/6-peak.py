#!/usr/bin/python3
"""a function that finds a peak in a list of unsorted integers"""

def find_peak(list_of_integers):
    """finds a peak"""
    if len(list_of_intege) == 0:
        return None
    elif len(list_of_intege) == 1:
        return list_of_intege[0]
    else:
        return (max(list_of_intege))
    

